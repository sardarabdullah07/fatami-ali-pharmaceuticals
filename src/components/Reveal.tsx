/**
 * Kept as the historical import path. The implementations now live in
 * `@/components/motion` alongside the rest of the motion system.
 */
export {
  Reveal,
  StaggerGroup as RevealGroup,
  StaggerItem as RevealItem,
  TextReveal,
  ImageReveal,
  Counter,
} from './motion'
